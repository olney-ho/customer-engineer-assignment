From: marc@hotmail.com (Free Algolia user)

Subject: Error on website

Hi, my website is not working and here's the error:
Can you fix it please?

![error message](./error.png)  

## Your Response

Hi Marc,

Thanks for including that screenshot, that definitely helps.

The error says **"Uncaught ReferenceError: searchList is not defined"**, which means your code is trying to use a variable or function called **searchList** that hasn't been declared or imported where it's being used.

1. **Typo or naming mismatch.** Make sure the variable name matches exactly between where it's defined and where it's called. JavaScript is case-sensitive, so `searchList`, `searchlist`, and `SearchList` are all treated as different things.

2. **Script loading order.** If `searchList` is defined in one JS file and used in another, the file that defines it needs to be loaded first in your HTML:
   ```html
   <!-- defines searchList, load first -->
   <script src="searchList.js"></script>
   <!-- uses searchList, load second -->
   <script src="app.js"></script>
   ```
   Could you share the relevant code files? That would let me find the exact line causing the problem. In the meantime, try searching your project for where `searchList` is defined and make sure it's accessible in the file where the error is thrown.

Let me know what you find!

Best Regards,

Olney Ho

## Your Comments (Optional): 
